<div class="filter mt-3">
   <p class="mb-2">Store</p>
   <%@stores.each do |row|%>
   <ul class="list-group">
      <li class="list-group-item list-group-item-action mb-2 "> <%=link_to row.name, page_path(row), class: "text-muted"%></span> </a></li>
      <%end%>
   </ul>
</div>